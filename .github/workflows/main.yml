name: Retrieve secrets 
on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: WebScrape

    steps:
      - name: Set up environment variable
        env:
          DROPBOX_ACCESS_TOKEN: ${{ secrets.DROPBOXTOKEN }}
        run: echo "DROPBOX_ACCESS_TOKEN is $DROPBOX_ACCESS_TOKEN"
